<script lang="ts">
    // Shown instead of a real photo until the editor uploads one — see
    // THEMES.md and the photo-honesty note in project memory: no fabricated
    // stock photography standing in for real GEKO photos. Two brand pills,
    // not the icon.svg wordmark — that mark's text is white-filled for use
    // on the dark footer, and reads as a broken smear on a light background.
</script>

<div class="flex h-full w-full items-center justify-center gap-1.5" aria-hidden="true">
    <span class="h-3 w-8 rounded-full bg-[var(--geko-amber)]"></span>
    <span class="h-3 w-8 rounded-full bg-[var(--geko-violet)]"></span>
</div>
